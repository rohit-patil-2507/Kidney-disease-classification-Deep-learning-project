pip>=26.1.1
tensorflow>=2.20.0
numpy>=1.26.4,<2.1.0
Flask>=3.0.3
Flask-Cors>=4.0.1
Pillow>=11.0.0
pandas>=2.2.2
matplotlib>=3.9.1
groq>=0.9.0
pydicom>=2.4.4
streamlit>=1.36.0
dvc>=3.51.2
mlflow>=2.14.3
scipy>=1.13.1
opencv-python>=4.10.0.84
PyYAML>=6.0.2
python-box>=7.2.0
joblib>=1.4.2
reportlab>=4.2.2
pdfkit>=1.0.0
scikit-learn>=1.5.1 # Keep if pdfkit dependency is removed or addressed
# If pdfkit is needed, you must find an alternative way to generate PDFs
# on Streamlit Cloud, as wkhtmltopdf (a system executable) cannot be installed via pip.
# Consider using a pure Python PDF library if reportlab isn't suitable,
# or generate PDFs elsewhere.

# The following are often development dependencies or tools that might not be needed
# for the deployed Streamlit app's runtime. Review and uncomment only if essential.
# Remove '-e .' as it's for editable installs, not typically for deployment.
# Remove 'wkhtmltopdf' as it's a system executable, not a Python package.
# Remove 'gdown' if files are pre-downloaded or served differently.
# Remove 'dvc' and 'mlflow' if they are not used at runtime by the Streamlit app.
#-e .
#gdown
#wkhtmltopdf==0.2
#dvc>=3.51.2
#mlflow>=2.14.3
